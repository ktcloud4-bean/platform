---
- name: OBS-16 SeaweedFS native metrics만 rollback
  hosts: object_nodes
  become: true
  tasks:
    - name: SeaweedFS metrics role 부분 rollback
      ansible.builtin.include_role:
        name: seaweedfs_s3
        tasks_from: metrics-rollback.yml

---
# BKP-01: 라이브 k3s를 중단하지 않고 SQLite 온라인 backup과 server token 보호를 선언한다.

- name: k3s SQLite datastore와 server token 전용 backup 선언
  hosts: k3s_servers
  gather_facts: true
  become: true
  roles:
    - k3s_datastore_backup

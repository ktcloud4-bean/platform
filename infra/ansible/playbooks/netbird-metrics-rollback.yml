---
- name: OBS-16 NetBird native metrics만 rollback
  hosts: netbird_server
  become: true
  tasks:
    - name: NetBird metrics role 부분 rollback
      ansible.builtin.include_role:
        name: netbird_server
        tasks_from: metrics-rollback.yml

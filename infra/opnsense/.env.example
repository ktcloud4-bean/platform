# infra/opnsense/.env 로 복사해 mode 600으로 저장한다.
# 값은 셸로 평가되지 않으며 줄바꿈 없는 literal 또는 한 쌍의 따옴표만 허용한다.
OPN_KEY=
OPN_SECRET=

# 생략하면 docs/ip-plan.md의 OPNsense canonical hostname을 사용한다.
OPN_URL=

# 사설 CA를 쓸 때만 지정한다. 현재 공인 인증서에는 필요하지 않다.
OPN_CACERT=

apiVersion: apps/v1
kind: Deployment
metadata:
  name: dashy
  namespace: pomerium
  annotations:
    argocd.argoproj.io/sync-wave: "0"
  labels:
    app.kubernetes.io/name: dashy
    app.kubernetes.io/component: portal
    app.kubernetes.io/part-of: platform-gitops
spec:
  replicas: 1
  revisionHistoryLimit: 2
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: dashy
      app.kubernetes.io/component: portal
  template:
    metadata:
      labels:
        app.kubernetes.io/name: dashy
        app.kubernetes.io/component: portal
        app.kubernetes.io/part-of: platform-gitops
    spec:
      automountServiceAccountToken: false
      terminationGracePeriodSeconds: 30
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: dashy
          image: harbor.imcherry5778.xyz/curated-platform/dashy@sha256:017227a623c9e90c21e372eda23f498e3e6a5b3863442d269c7f96e250e3883a
          imagePullPolicy: IfNotPresent
          env:
            - name: NODE_ENV
              value: production
            - name: HOST
              value: 0.0.0.0
            - name: PORT
              value: "8080"
            - name: DISABLE_CONFIG_BACKUPS
              value: "true"
            - name: DISABLE_PROXY_ENDPOINTS
              value: "true"
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          startupProbe:
            httpGet:
              path: /healthz
              port: http
              scheme: HTTP
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 60
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
              scheme: HTTP
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
              scheme: HTTP
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          volumeMounts:
            - name: dashy-config
              mountPath: /app/user-data
              readOnly: true
            - name: dashy-tmp
              mountPath: /tmp
            - name: dashy-cache
              mountPath: /app/.cache
      volumes:
        - name: dashy-config
          configMap:
            name: dashy-config
        - name: dashy-tmp
          emptyDir:
            sizeLimit: 64Mi
        - name: dashy-cache
          emptyDir:
            sizeLimit: 64Mi
